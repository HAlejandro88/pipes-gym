import { VisualizarPipe } from './visualizar.pipe';

describe('VisualizarPipe', () => {
  it('create an instance', () => {
    const pipe = new VisualizarPipe();
    expect(pipe).toBeTruthy();
  });
});
